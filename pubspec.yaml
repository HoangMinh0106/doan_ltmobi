name: doan_ltmobi
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies: # ✅ Đặt ở đây (cùng cấp với flutter)
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.2 
  mongo_dart: ^0.10.5
  image_picker: ^1.1.2
  url_launcher: ^6.3.1 #dùng mở url trình duyệt trên phone
  mailer: ^6.0.1 #dùng để gửi email thật
  random_string: ^2.3.1 #tạo mã OTP
  pinput: ^4.0.0 #giao diện OTP
  elegant_notification: ^2.2.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  # Các cấu hình khác (assets, fonts...) nếu có
  assets:
    - assets/
